# ★★★ HTTP -> HTTPS ★★★
server {
    listen 80;
    listen [::]:80;
    server_name suzookaizokuhunter.com www.suzookaizokuhunter.com;
    return 301 https://$host$request_uri;
}

# ★★★ HTTPS ★★★
server {
    listen 443 ssl;
    listen [::]:443 ssl;
    http2 on;  # 使用 HTTP/2

    server_name suzookaizokuhunter.com www.suzookaizokuhunter.com;

    # SSL 憑證路徑 (需放在主機 /etc/letsencrypt/live/suzookaizokuhunter.com/)
    ssl_certificate     /etc/nginx/certs/live/suzookaizokuhunter.com/fullchain.pem;
    ssl_certificate_key /etc/nginx/certs/live/suzookaizokuhunter.com/privkey.pem;

    # 一般 SSL 優化
    ssl_session_timeout 5m;
    ssl_session_cache   shared:SSL:50m;
    ssl_protocols       TLSv1.2 TLSv1.3;
    ssl_ciphers         HIGH:!aNULL:!MD5;
    ssl_prefer_server_ciphers on;
    add_header Strict-Transport-Security "max-age=31536000; includeSubDomains" always;

    # 前端檔案根目錄
    root   /usr/share/nginx/html;
    index  index.html index.htm;

    # ★ SPA fallback
    location / {
        try_files $uri $uri/ /index.html =404;
    }

    # ★ 轉發給 Express (注意 Express 在 Docker 內的服務名稱 suzoo_express:3000)
    location /api/ {
        proxy_pass http://suzoo_express:3000;
    }

    # ★ 轉發給 FastAPI (suzoo_fastapi:8000)
    location /api/py/ {
        proxy_pass http://suzoo_fastapi:8000;
    }

    # ★ 可選：對靜態檔案做長效快取
    location ~* \.(js|css|png|jpg|jpeg|gif|svg|ico)$ {
        expires 1y;
        add_header Cache-Control "public, immutable";
    }
}
